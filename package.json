{
  "name": "finwellai-site",
  "private": true,
  "description": "Finwell AI marketing + waitlist site (Netlify static).",
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test tests/smoke.spec.js",
    "test:e2e": "playwright test tests/e2e.spec.js",
    "test:form": "playwright test tests/form.spec.js",
    "test:regression": "playwright test tests/regression.spec.js",
    "test:ui": "playwright test --ui",
    "test:install": "playwright install --with-deps chromium",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
